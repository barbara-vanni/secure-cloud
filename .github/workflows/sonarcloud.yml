name: SonarCloud

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  sonar:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install build tools
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential cmake ninja-build unzip curl git

      # >>> AJOUT 1/2 : installer vcpkg
      - name: Install vcpkg
        run: |
          git clone https://github.com/microsoft/vcpkg.git "${{ github.workspace }}/vcpkg"
          "${{ github.workspace }}/vcpkg/bootstrap-vcpkg.sh" -disableMetrics

      # >>> AJOUT 2/2 : installer Drogon via vcpkg (et deps)
      - name: Install Drogon (vcpkg)
        run: |
          "${{ github.workspace }}/vcpkg/vcpkg" install drogon --triplet x64-linux
        # (optionnel) activer un cache vcpkg pour accélérer les runs suivants

      - name: Download build-wrapper
        run: |
          curl -sSLo bw.zip https://sonarcloud.io/static/cpp/build-wrapper-linux-x86.zip
          unzip -q bw.zip

      # Configure (NON wrapé) — on indique le toolchain vcpkg à CMake
      - name: Configure project
        run: |
          rm -rf build bw-output
          cmake -S . -B build -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_TOOLCHAIN_FILE="${{ github.workspace }}/vcpkg/scripts/buildsystems/vcpkg.cmake" \
            -DVCPKG_TARGET_TRIPLET=x64-linux

      # Build (WRAPÉ) — alimente bw-output/
      - name: Build (wrapped)
        run: |
          ./build-wrapper-linux-x86/build-wrapper-linux-x86-64 \
            --out-dir bw-output \
            ninja -C build

      # Ton step SonarCloud existant (inchangé)
      - name: Run SonarCloud Scan
        uses: SonarSource/sonarcloud-github-action@v2
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          args: >
            -Dsonar.host.url=https://sonarcloud.io
            -Dsonar.organization=secure-cloud
            -Dsonar.projectKey=barbara-vanni_secure-cloud
            -Dsonar.projectVersion=${{ github.run_number }}
            -Dsonar.cfamily.build-wrapper-output=bw-output
            -Dsonar.exclusions=${{ steps.excl.outputs.exclusions }}
            -Dsonar.verbose=true
