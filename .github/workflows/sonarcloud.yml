name: SonarCloud

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  sonar:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install build tools
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential cmake ninja-build unzip curl git

      - name: Install vcpkg
        run: |
          git clone https://github.com/microsoft/vcpkg.git "${{ github.workspace }}/vcpkg"
          "${{ github.workspace }}/vcpkg/bootstrap-vcpkg.sh" -disableMetrics

      - name: Install C++ deps (vcpkg)
        run: |
          "${{ github.workspace }}/vcpkg/vcpkg" install drogon curl[openssl] nlohmann-json --triplet x64-linux

      - name: Download build-wrapper
        run: |
          curl -sSLo bw.zip https://sonarcloud.io/static/cpp/build-wrapper-linux-x86.zip
          unzip -q bw.zip

      - name: Configure project
        run: |
          rm -rf build bw-output
          cmake -S . -B build -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_TOOLCHAIN_FILE="${{ github.workspace }}/vcpkg/scripts/buildsystems/vcpkg.cmake" \
            -DVCPKG_TARGET_TRIPLET=x64-linux \
            -DCMAKE_FIND_PACKAGE_PREFER_CONFIG=ON

      - name: Build (wrapped)
        run: |
          ./build-wrapper-linux-x86/build-wrapper-linux-x86-64 \
            --out-dir bw-output \
            ninja -C build

      # (Optionnel) Inspecter le fichier pour s'assurer que les chemins sont ceux du runner,
      # c.-à-d. /home/runner/work/secure-cloud/secure-cloud/...
      - name: Show first entries of compile_commands.json
        run: |
          head -n 100 bw-output/compile_commands.json || true

      - name: Download SonarScanner CLI
        run: |
          SCAN_VER="5.0.1.3006"
          curl -sSLo sonar-scanner.zip "https://binaries.sonarsource.com/Distribution/sonar-scanner-cli/sonar-scanner-cli-${SCAN_VER}-linux.zip"
          unzip -q sonar-scanner.zip
          mv "sonar-scanner-${SCAN_VER}-linux" sonar-scanner
          java -version

      - name: Run SonarCloud Scan (CLI)
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: |
          ./sonar-scanner/bin/sonar-scanner \
            -Dsonar.host.url=https://sonarcloud.io \
            -Dsonar.organization=secure-cloud \
            -Dsonar.projectKey=barbara-vanni_secure-cloud \
            -Dsonar.projectVersion=${{ github.run_number }} \
            -Dsonar.cfamily.compile-commands=bw-output/compile_commands.json \
            -Dsonar.verbose=true